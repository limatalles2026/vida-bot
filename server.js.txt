import express from "express";

const app = express();
app.use(express.json());

app.post("/webhook", (req, res) => {
  console.log(req.body);
  res.json({ reply: "Bot funcionando 🚀" });
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log("Rodando"));